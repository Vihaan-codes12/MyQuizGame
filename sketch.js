var canvas;
var gameState = 0;
var database, quiz, question;
var contestantCount = 0;
var contestant;

function setup(){
  canvas = createCanvas(850,400);
}


function draw(){
  background("pink");

  
}
